function Button({ buttonText }) {
    return (
        <button style={{
            backGroundColor: "#007BFF",
            color: "#007BFF",
        }}>
            {buttonText}
        </button>

    )
}